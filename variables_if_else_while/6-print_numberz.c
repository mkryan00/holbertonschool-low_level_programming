#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9
 * Return: Always return 0 (Success)
 */
int main(void)
{
int l;

for (l = '0'; l <= '9'; l++)

{
putchar(l);
}

putchar('\n');

return (0);
}
